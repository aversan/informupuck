.nano { background: #bba; width: 500px; height: 500px; }
/*.nano .nano-content { padding: 10px; }*/
.nano .nano-pane   { background: #888; }
.nano .nano-slider { background: #111; }
/** initial setup **/
.nano {
	position : relative;
	width    : 100%;
	height   : 100%;
	overflow : hidden;
}
.nano > .nano-content {
	position      : absolute;
	overflow      : scroll;
	overflow-x    : hidden;
	top           : 0;
	right         : 0;
	bottom        : 0;
	left          : 0;
}
.nano > .nano-content:focus {
	outline: thin dotted;
}
.nano > .nano-content::-webkit-scrollbar {
	display: none;
}
.has-scrollbar > .nano-content::-webkit-scrollbar {
	display: block;
}
.nano > .nano-pane {
	  background: rgba(0,0,0,.25);
	position: absolute;
	width: 7px;
	right: 0;
	top: 0;
	bottom: 0;
	visibility: hidden\9;
	opacity: .01;
	-webkit-transition: .2s;
	-moz-transition: .2s;
	-o-transition: .2s;
	transition: .2s;
	-moz-border-radius: 5px;
	/* -webkit-border-radius: 5px; */
	/* border-radius: 5px; */
}
.nano > .nano-pane > .nano-slider {
	background: #4D739F;
	background: rgb(77, 115, 159);
	position: relative;
	margin: 0 1px;
	-moz-border-radius: 3px;
	/* -webkit-border-radius: 3px; */
	/* border-radius: 3px; */
}
.nano:hover > .nano-pane, .nano-pane.active, .nano-pane.flashed {
	visibility : visible\9; /* Target only IE7 and IE8 with this hack */
	opacity    : 0.99;
}

.nano.layout-nano-content
.nano.layout-nano-catalog-col-box
.nano.layout-nano-cell-one-col
.nano.teaser-section__list
.nano.layout-nano-card-list
	+below(767px)
		min-height initial !important
		height auto !important
		max-height none !important

.nano.layout-nano-content > .nano-content
.nano.layout-nano-catalog-col-box > .nano-content
.nano.layout-nano-cell-one-col > .nano-content
.nano.teaser-section__list > .nano-content
.nano.layout-nano-card-list > .nano-content
	+below(767px)
		position static !important
		overflow visible !important
		margin-right 0 !important

.nano.layout-nano-content > .nano-pane
.nano.layout-nano-catalog-col-box > .nano-pane
.nano.layout-nano-cell-one-col > .nano-pane
.nano.teaser-section__list > .nano-pane
.nano.layout-nano-card-list > .nano-pane
	+below(767px)
		display none !important
